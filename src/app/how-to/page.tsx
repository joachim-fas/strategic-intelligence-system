/**
 * /how-to — Server Component wrapper.
 * Interactive content lives in HowToClient (client component).
 */

import HowToClient from "./HowToClient";

export default function HowTo() {
  return <HowToClient />;
}
