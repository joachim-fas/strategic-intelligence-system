/**
 * Sign-in page — Server Component wrapper.
 * Interactive form lives in SignInClient (client component).
 */

import SignInClient from "./SignInClient";

export default function SignInPage() {
  return <SignInClient />;
}
