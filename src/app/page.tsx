/**
 * Home page — Server Component wrapper.
 * Interactive content lives in HomeClient (client component).
 */

import HomeClient from "./HomeClient";

export default function Home() {
  return <HomeClient />;
}
