/**
 * /verstehen — Knowledge Cockpit (Server Component wrapper).
 * Interactive content lives in VerstehenClient (client component).
 */

import VerstehenClient from "./VerstehenClient";

export default function VerstehenPage() {
  return <VerstehenClient />;
}
